os.loadAPI("build")
os.loadAPI("build")
--build a grid system for easy building
--usage: grid(size,spacing)
print("rows/cols? ")
size=read()
print("blocks between grid points?")
space=read()
build.grid(size,space)
gps.goto(0,0,0)
